# Empty designator-less verse runs get a fix (Will, 2026-08-25)

`\v \v \v 1 Put…` — post-gate these are designator-less verses, each
firing verse-without-designator. The RemoveOrphanEmptyVerse refusal
does NOT apply: a designator-less empty `\v` carries no identity and
no content — deleting it loses nothing nameable. PROPOSED:
verse-without-designator gains a fix ONLY when the marker is EMPTY
(ws-only to the next verse/chapter/para marker): delete the extent; a
consecutive empty run collapses under ONE fix anchored on the first
(pass-12 chain pattern verbatim, same site-repaired reasoning).
`\v Then text` (designator-less WITH content) stays fixless — verse
identity is the human's, via the editor's propped-open slot.
Formatter bit on (empty-paragraph's dual-citizen shape). Queue with
this file's pass (they share the ancestry/emptiness machinery).

Shares the ancestry/emptiness machinery with verse-under-heading.md —
build the two in one pass.
