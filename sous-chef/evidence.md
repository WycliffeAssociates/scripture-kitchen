# Evidence ledger

Every measurement Sous Chef 2's architecture rests on. Append rows; never
rewrite one. A row that a later run contradicts gets a *new* row saying so —
the point of a ledger is that the old number stays readable next to the new.

Rows are evidence, not production guarantees. Rerun the named command when a
production choice depends on one.

Machine `M1` is Apple Silicon, one core, the committed 8-corpus test tier in
`../corpora/`. Rejected implementations measured here are kept, with their
numbers, in [experiments/](experiments/).

## Roofline and differential runs

| date | question | machine · command | observed result | consequence |
| --- | --- | --- | --- | --- |
| 2026-09-01 | roofline, production hygiene | M1 · `cargo bench -p sous-core` | scalar dependent chain 1.05 GB/s; autovectorized compare 10 GB/s; `memchr3` 31 GB/s on clean text, 5.6 GB/s on French (0xC2 NBSP/guillemet density); `hygiene::scan` 8.6 GB/s on six corpora, 6.6 GB/s Greek, 3.8 GB/s French. Replacing three `memmem` marker passes with one `memchr3` took it from 5.8 to 8.6 GB/s | every pass is measured against the vectorized ceiling; the needle filter's hit density, not the range filter, is the remaining cost |
| 2026-09-02 | roofline, Stage 1 classifier walk (medians, ns/scalar) | M1 · `cargo bench -p sous-core` | plain lookup (static two-level) 1.64 en/nya/swh, 1.83 spa, 2.00 fra, 3.66 amh, 3.90 hin, 4.17 grc. Lazy 128 KiB flat BMP 1.97/2.14/2.26/3.36/3.57/3.79 — faster on non-Latin by 8-10%, slower on Latin by 17%. Byte trie over raw UTF-8 1.31/1.51/1.65/3.23/3.46/3.89: faster than the plain lookup on every corpus. Byte trie plus 8-byte SWAR ASCII with hysteresis 0.22 en/nya, 0.36 swh, 1.49 spa, 1.76 fra, 3.40 amh, 3.62 hin, 4.01 grc. SWAR over the *decoded* lookup taxes amh/hin 24% and grc 13% | **Table:** static two-level wins the near tie on size — 28.7 KiB of `.rodata`, no heap, no `OnceLock`, nothing extra in the `.wasm`, against 128 KiB built at first use. **Fast lanes:** the byte trie ships, and the SWAR ASCII chunk ships on top of it — no corpus in the tier is slower than the plain lookup, and the chunk costs the bare trie only 3-7% on non-Latin. The decoding SWAR variant is rejected: v1's failure mode reproduced exactly, and taxing Indic and Greek to speed English is the wrong trade |
| 2026-09-02 | Stage 1 classifier walk, re-run after the losers were retired to `experiments/` | M1 · `cargo bench -p sous-core`, twice | Whole machine reads ~25% below the row above: `memchr3` 24 GB/s (was 31), autovectorized compare 7.8 GB/s (was 10). Within that, the plain two-level walk and the bare byte trie **swap order** — 1.12 vs 1.64 ns/scalar on English, stable across both runs, where the row above has 1.64 vs 1.31. The shipped `walk_trie_swar` lane is unchanged at 0.27 ns/scalar English and still fastest on every Latin corpus | **Not adjudicated.** No shipped code path changed in the retirement; `hygiene::scan` still rides the trie plus SWAR chunk. Either the machine state or the removal of the `Lookup` enum from the bench's own subject moved the two-level row. Re-measure both walks on a quiet machine before any promotion argument leans on their order |
| 2026-09-02 | atom rule fleet differential | `cargo run -p sous-core --release --example atom_fleet`, over the donor's `ebible-main/corpus` checkout because `../corpora/calibration-corpora/` is not present on this machine | 1,079 corpora, 1,809,448,088 UAX #29 clusters, 0 split by the atom rule, 0 corpora affected | the conservative widening rule is safe to ship with no runtime segmenter. GB9c is the one place it needs help: without the linker bit, 102,139 Hindi clusters (3.6% of `hin2017`) split |
| 2026-09-02 | Expediter publication cost, warm and after a keystroke | M1 · `cargo bench -p usfm_galley --bench expediter`, over the committed 66-book `testData/exampleCorpora/en_ulb` (4,506,101 bytes, 1,189 chapters) with `Hygiene` as the pass | `publish_unchanged` (nothing updated: chapter-table hits, reduce, rebase through the retained mask and `Utf16Table`, encode — no text read, no chapter mapped) median **14.43 µs**, range 13.91–14.74. `edit_one_chapter_then_publish` (one character typed into a middle chapter of 42-MRK.usfm, 83,733 bytes: `Pantry::update`'s lex/CST/mask/UTF-16 re-derivation for that book, one chapter re-mapped, then the same publish) median **396.6 µs**, range 361.7–507.1, 20 samples each | A whole-corpus republication is 0.09% of a 16 ms frame, so the host may publish on every keystroke rather than debounce. The keystroke row is dominated by `update`'s per-book re-derivation, not by Sous: the publish half of it is the 14 µs above plus one chapter's map. Slice C's parallel map has nothing to win here; if this row ever hurts, it is `Pantry::update` to attack |
| 2026-09-02 | Expediter publication cost once C1's mark-and-sweep with generation retention is in | M1 · `cargo bench -p usfm_galley --bench expediter`, same 66-book `testData/exampleCorpora/en_ulb` and `Hygiene` pass as the row above | `publish_unchanged` median **14.43 µs** (14.24–19.24) — identical to the pre-sweep row, because a publication that added no chapter table and aged no ring skips the sweep walk entirely. `edit_one_chapter_then_publish` median **399.6 µs** (385.1–561). The new `publish_after_sweep_churn` (the same keystroke after 20 earlier edits each published, so MRK's ring is at its full five tables and the sweep marks 70 tables) median **398.4 µs** (380.0–454.2), 20 samples each. The first cut, sweeping unconditionally, put `publish_unchanged` at **41.31 µs** — 2.9× | Sweeping costs nothing measurable at the grain that matters: a full ring is within noise of a fresh cache on the keystroke row, and the warm republication is unchanged. The 41 µs cut is why the sweep is guarded by a dirty bit rather than run every publication — marking 1,189 chapter rows to free nothing is three times the whole warm publish |
| 2026-09-02 | JS→WASM string marshaling cost (`TextEncoder.encodeInto` into a `WebAssembly.Memory` view, the wasm-bindgen path; medians of 10, 3 for 250 MB) | M1 Max · Node 24.4.1, throwaway script in the scratchpad | ~5 KB chapter 4–5 µs; ~150 KB book 0.10 ms ASCII / 0.15 ms 50% Devanagari; ~5 MB corpus 3.5 / 4.9 ms; 250 MB pathological 179 / 258 ms. Throughput flat with size: ~1.4 GB/s UTF-8 out ASCII, ~1.0 GB/s mixed. `encodeInto` is 1.4–1.6× faster than allocating `encode`; pure scan (`Buffer.byteLength`) is 4–5× faster than either, so the write dominates. `Memory.grow` for 250 MB: 0.25 ms once. Peak RSS with both 250 MB variants held: 1.5 GB | Resending a whole 5 MB corpus per keystroke costs 3.5–5 ms of a 16 ms frame — confirms keyed whole-book `update` (0.1–0.15 ms per book). A `find` that passes one book's text is free; whole-corpus find on a normal Bible is a few ms per query, acceptable for an explicit action; the 250 MB aligned target is ~0.2 s per query. Text pinning stays deferred: WASM linear memory never shrinks, so pinning inflates the instance permanently, a device decision no library should make |
| 2026-09-02 | observation size for the Level 1b substrate and the Stage 4 word lanes (per chapter, 8-corpus tier) | M1 · `cargo run -p sous-core --release --example observation_size` | Scalar lanes (scalar inventory + G0 pair triples + run shapes, sorted vectors): median 560–1,300 B/chapter, 0.3–1.2 MB per Bible, 10–20% of raw text; per chapter 5–13 distinct nonletter scalars, 6–21 pair triples, 5–18 run shapes. Words: 220–260 distinct folded words per chapter median; per Bible lane A (packed u128 + flags, chapter grain) 6.7–8.6 MB (1.5–2× raw), lane B (u64 hash, chapter grain) 3.2–4.1 MB, lane C (u64 hash, book grain) 1.0–1.7 MB (25–40% of raw). Words fit a u64 12–23% of the time in Amharic/Greek/Hindi (67–100% Latin), a u128 69–80% there | Scalar observations are cheap; the 2 KB/chapter target holds with room. Packed word keys are a Latin optimization, so Stage 4 keys words by hash; chapter vs book grain for the word lane is Stage 4's opening decision with these numbers |
| 2026-09-02 | does mapping a book's chapters in parallel pay? | M1 · `cargo bench -p usfm_galley --bench expediter`, with and without `--features parallel`, same 66-book `testData/exampleCorpora/en_ulb` and `Hygiene` pass as the rows above | The new `publish_cold` row — a fresh Expediter over all 66 books, registration outside the timer, so every one of the 1,189 chapters is mapped — is **5.63 ms** serial (5.52–5.83) and **9.72 ms** parallel (8.72–10.14), 1.7× slower. `RAYON_NUM_THREADS=1` gives 6.85 ms and `=4` gives 7.49 ms, so ~1.2 ms of it is the queue-and-collect overhead and the rest grows with thread count. `publish_unchanged` (14.6 vs 15.1 µs) and `edit_one_chapter_then_publish` (393 vs 416 µs) are within noise either way — neither maps enough chapters to care | The `parallel` feature ships opt-in and OFF. For `Hygiene` the map is ~0.5 ms of that 5.6 ms (8.6 GB/s over 4.5 MB of projection) and allocates one `Vec` per chapter, so work-stealing and allocator contention cost more than the whole map saves. The gate keeps the two paths byte-identical (`galley/tests/equivalence.rs`) so a costlier pass can switch it on; the first thing to change then is one `par_iter` over the whole corpus instead of one per book, which amortizes 66 joins into one |
| 2026-09-02 | Level 1b substrate walk (D1a): what one scalar walk per chapter costs, and what its row weighs | M1 · `cargo bench -p sous-core -- substrate`, and `cargo test -p sous-core --release --test substrate_reference -- --include-ignored` | **`substrate_map`** medians, ns/scalar (MB/s): nya 7.48 (134), swh 7.76 (130), en 8.26 (121), spa 10.0 (103), fra 11.0 (96), amh 14.7 (176), hin 15.1 (167), grc 15.5 (128). **`substrate_reduce`** (merge every lane of a whole corpus and fix every seam) 0.56-1.24 µs per chapter, 0.29-1.17 ms per corpus. **Allocations** 9.5-10 per `map` plus ~1.4 grows and ~4.3 shrinks, 15 allocator ops per chapter, 10 live at peak. **Row bytes** (inline + heap, real `ChapterRow`s over the tier): median 964, p90 1,324; per-corpus medians spa 828, en 884-920, nya 972, fra 1,040, hin 1,096, amh 1,412, grc 1,752. Two subtractions on the way: hoisting the 24-byte per-scalar state out of the counter struct into a local was 20-27% on every corpus (heap writes were forcing a reload of all eleven fields), and a 512-slot open-addressed table in front of the non-ASCII inventory was ~14% on Amharic and nothing on Latin | The walk is 8-15 ns/scalar against the bare classifier's 0.27-4.0 ns — the counting, not the classification, is the cost, which is exactly why the roadmap allows only one such walk. A whole Bible is ~33 ms cold and one chapter ~26 µs, so a keystroke is nothing next to `Pantry::update`'s 400 µs. The 1.5 KB/chapter median and 2 KB p90 budgets hold with room, and the two corpora over 1.5 KB (Greek 1,752, Amharic 1,412) are over it on the dense letter census alone, which is the ruled cost of letting a stray letter reach the rarity roster. Next levers if this ever hurts: the per-call allocations (a `map_with(&mut Scratch)` seam), and the `finish` sort, which is 9-24% of the map |
| 2026-09-02 | does the parallel map pay once the pass is the substrate walk? | M1 · `cargo bench -p usfm_galley --bench expediter` with the bench's pass swapped from `Hygiene` to `Substrate` (a local edit, not kept), with and without `--features parallel`, same 66-book `en_ulb` | `publish_cold` **30.8 ms** serial (30.75-30.96) vs **16.2 ms** parallel (15.5-17.4), 1.9× faster on the per-book `par_iter` as shipped. `edit_one_chapter_then_publish` 1.20 vs 1.18 ms and `publish_after_sweep_churn` 1.15 vs 1.21 ms, noise either way. `publish_unchanged` is **697 µs** with the substrate against 14 µs with `Hygiene`: every publish re-runs `fold_book` over all 1,189 rows | At 26 µs per chapter the map is past the point where scheduling overhead wins, so the feature pays for a cold open and is still a wash on a keystroke; it stays opt-in until a host asks for the cold number. The real cost the swap exposed is the reduce, not the map: 0.7 ms of merges on every publish with nothing changed. D1b should decide whether book aggregates are cached per (book, chapter-set) and only re-folded for dirty books, before the substrate rides in the resident host |
| 2026-09-03 | D1b: what does moving hygiene's four scalar classes onto the substrate walk cost the walk? | M1 · `cargo bench -p sous-core -- substrate_map`, three A/B rounds of the same binary built with the site gate compiled out and with it in, best-of-three fastest per corpus. The machine was NOT quiet — a concurrent whole-workspace build held load average 8-13 — so the A/B ratio is the reading, not the absolute times | Site gate OFF (ms, fastest of 3): en 25.86, amh 5.40, fra 40.08, grc 10.29, hin 43.89, nya 20.65, spa 29.26, swh 24.21. Gate ON, shipped: en 26.65 (**+3.1%**), amh 5.70 (+5.5%), fra 42.14 (+5.1%), grc 10.86 (+5.5%), hin 48.18 (+9.8%), nya 21.41 (+3.7%), spa 31.01 (+6.0%), swh 24.97 (+3.1%). Two gate designs were measured against the same OFF baseline: reading `ScalarSites::pending()` off the counter struct costs en +6.9%, nya +6.5%, swh +6.4%; mirroring it into the register-resident `Hot` local halves that (en +3.1%, nya +3.7%, swh +3.1%) at ~1pt on Indic and Greek, and ships. A third cut that also pre-filtered marks in the gate (`mark_is_free_behind(prev)`, with the open run mirrored too) was **slower than both on every corpus** — hin 50.06 against 48.18 — and is rejected: the extra bit-test chain and the second mirror store cost more than the calls they skip | Row bytes moved by the 16 inline bytes and nothing else — tier median 964 -> 980 B, p90 1,324 -> 1,340 B, and 11 sites in 7,607 chapters. The lane is 3-6% on Latin and Greek and 10% on Devanagari, against a budget of 5% and 10%. Devanagari is at budget and Spanish/French are ~1pt over it: the cost is real, and it is where the classes are — `SUSPECT` includes MARK, so `ScalarSites::step` is genuinely called on every Indic matra. The subtraction that mattered was the gate's residency, not its shape. Next lever if this ever hurts is a mark filter that does not have to mirror the open run |
| 2026-09-03 | D1b: what is left of `hygiene::scan` once it is the three byte sweeps alone? | M1 · `cargo bench -p sous-core -- hygiene`, against the same command's 2026-09-01 row | **8.23 GB/s** en, 8.33 nya, 8.28 swh, 8.02 hin, 7.89 amh, 7.37 spa, 6.57 grc, 3.67 fra (medians). The 2026-09-01 row measured the same call at 8.6 GB/s on six corpora, 6.6 Greek, 3.8 French — with `scan_scalars` still in it. Before this pass, on this machine and under this load, the same call read 3.90 GB/s en / 0.58 amh / 0.45 fra / 0.37 hin | Byte-only hygiene is back at its 2026-09-01 shape: the classifier walk it had grown is now `substrate_map`'s cost and shows up there. French stays the outlier for the same reason it always did — 0xC2 leads both NBSP and the guillemets, so the `memchr3` needle filter has real hit density there, not because of anything the scalar classes were doing |
| 2026-09-03 | D1b: what does the resident host cost once the pass is the product tuple and each book's reduce output is cached by `RawChecksum`? | M1 · `cargo bench -p usfm_galley --bench expediter`, with and without `--features parallel`, same 66-book `testData/exampleCorpora/en_ulb` (1,189 chapters), pass now `Brigade` = `(HygieneBytes, Substrate)`. Load average 9-11 from a concurrent build | `publish_unchanged` **6.87 µs** serial (6.75-11.79), 7.37 µs parallel — against **697 µs** for the substrate walk without the cache (2026-09-02) and 14.4 µs for `Hygiene` alone. `edit_one_chapter_then_publish` **434.9 µs** serial (416-581), 455.5 µs parallel. `publish_after_sweep_churn` **429.3 µs** serial, 505.1 µs parallel. `publish_cold` **33.2 ms** serial (32.6-40.9) vs **24.5 ms** parallel (21.0-37.2), 1.36× — the 2026-09-02 substrate row got 1.9× on a quiet machine, and the parallel row is the one a loaded machine punishes | The 697 µs the 2026-09-02 row flagged is gone: a republication with nothing updated now reduces no book at all and replays 1,189 chapters' worth of rows in 6.9 µs — half what `Hygiene` cost before D1b, because the replay skips the map-order walk as well as the fold. A keystroke is 435 µs, still dominated by `Pantry::update`'s per-book re-derivation rather than by Sous. `publish_cold` at 33 ms is the substrate walk's own cost plus the tuple's second reduce; parallel still pays for a cold open and is a wash on a keystroke, so the feature stays opt-in |
| 2026-09-03 | D2a-1: does splitting fold from judge cost the resident host anything? | M1 · `cargo bench -p usfm_galley --bench expediter`, with and without `--features parallel`, same 66-book `testData/exampleCorpora/en_ulb` (1,189 chapters), pass `Brigade`. `publish` now folds only the books whose `RawChecksum` has no cached `P::Aggregate` and judges every Target book on every call, then `Findings::finish` sorts. Load average 13-16 from a concurrent build | Serial medians: `publish_unchanged` **7.58 µs** (7.46-8.21), `edit_one_chapter_then_publish` **444.5 µs** (428.7-601.4), `publish_after_sweep_churn` **434.4 µs** (414.2-544.4), `publish_cold` **33.94 ms** (33.59-38.22). Parallel medians: 7.81 µs, 536.2 µs, 514.9 µs, 28.82 ms — but that run's spreads are junk (`publish_cold` 20.81-231.7 ms, `publish_unchanged` 7.62-129.2 µs), so only its medians are readable. Against the D1b row on the same command: `publish_unchanged` 6.87 -> 7.58 µs serial, `edit` 434.9 -> 444.5, churn 429.3 -> 434.4, cold 33.2 -> 33.9 ms | Within noise of D1b, and the ~0.7 µs on `publish_unchanged` is the price of the shape rather than of a regression: the old cache replayed a flat `(from, to, kind)` list per book, and judging now walks each cached aggregate's hygiene lane and then sorts the whole row set once in `finish`. 7.6 µs for a 66-book republication is still 0.05% of a 16 ms frame, so a corpus-level judge on every publish costs the host nothing it can feel — which is what D2a-2 needs, since a convention is a corpus fact and cannot be judged one book at a time. The parallel column is not adjudicated here; re-measure it on a quiet machine before any claim leans on it |
| 2026-09-03 | D2a-2: what does judging every book's counts on every publication cost the resident host? | M1 · `cargo bench -p usfm_galley --bench expediter`, with and without `--features parallel`, same 66-book `testData/exampleCorpora/en_ulb` (1,189 chapters), pass `Brigade`. `judge` now merges every aggregate's `scalars`, `pairs`, and `runs` lanes into corpus totals and walks the G0/G1/G3/rarity channels. Load average 13-14 from a concurrent build, the same band as the D2a-1 row | Serial medians: `publish_unchanged` **91.49 µs** (87.29-117.3), `edit_one_chapter_then_publish` **593.3 µs** (536.1-787.1), `publish_after_sweep_churn` **525.7 µs** (516.2-611.2), `publish_cold` **32.45 ms** (32.22-33.26). Parallel medians: 93.39 µs, 564.1 µs, 524.5 µs, **15.77 ms** (15.13-17.95), 2.06x on the cold open. Against D2a-1 on the same command: `publish_unchanged` 7.58 -> 91.49 µs (**12x**), `edit` 444.5 -> 593.3, churn 434.4 -> 525.7, cold 33.94 -> 32.45 ms | The 12x on `publish_unchanged` is judging itself, not a regression in the cache: the fold is still skipped for every book, and what the 84 µs buys is a corpus-wide merge of 66 books' three counting lanes plus the channel walk over every nonletter glyph, on every publication. 91 µs is 0.6% of a 16 ms frame, so the host may still publish on a keystroke; the keystroke row is 593 µs and still dominated by `Pantry::update`. If this ever hurts, the merge is the lever — the lanes arrive sorted per book and are re-sorted here as one flat vector, so a k-way merge or a corpus-total cache keyed by the set of `RawChecksum`s would take most of it back. Not spent now: a whole-corpus judge on every publish is what a convention being a corpus fact costs, and it is under a tenth of a millisecond |
| 2026-09-03 | D2a-2 fleet gate: how many patterns does the default staircase fire per corpus, against the v1 band sweep's comparator? | M1/M2 · `cargo run -p sous-core --release --example pattern_volume`, the 8-corpus tier then the 1,504-corpus vref tier at the sibling `scripture-sous-chef/corpora/vref` (`corpora/calibration-corpora/` is not on this machine). Default `JudgingConfig`; channels G0 (per side), G1, G3, rarity | Tier totals (exact/runshape/placement/rarity): WA-en-ulb 75 (21/26/22/6), amh 23 (0/1/5/17), francl 95 (15/28/46/6), grcsr 41 (5/8/16/12), hin2017 74 (28/18/27/1), nya 202 (74/64/47/17), spaRV1909 32 (3/8/18/3), swhulb 129 (40/42/36/11). Fleet, 1,504 corpora attempted and 0 skipped, patterns per corpus (p50/p90/p95/max): **total 45 / 91 / 105 / 201**; exact-neighbor 8/27/34/92; run-shape 15/29/35/52; placement 18/28/32/52; rarity 3/10/14/63. The comparator is p50 10 / p90 36 / p95 44 rows per corpus | **Reported, not tuned.** Volume is 4.5x the comparator at p50 and 2.4-2.5x at p90/p95, and the shapes of the two numbers are not the same thing: the comparator counted v1's *finding rows* per corpus, while this counts *pattern rows* — one per (channel, glyph, key) — and `Placement` here is two marginal distributions per glyph rather than one attached/spaced verdict, so a glyph that fires on both sides costs two rows. The widest rows on the ten largest corpora are all ordinary punctuation at 0.1-0.9% minority shares (comma before a digit, danda before a quote), which is the band doing what the band is for, not a broken denominator. Two things to adjudicate before D2b: whether the two placement sides should union into one row, and whether the p90 tail is corpora that genuinely mix two conventions. Do not move the staircase on this row alone |
| 2026-09-04 | D2b: how many distinct needles, hits, and sites does a site rescan carry per book, and what does one whole-corpus rescan cost? | M1 · `cargo bench -p sous-core -- sites_locate`, the committed 8-corpus tier, default `JudgingConfig`; one `memmem::Finder` per distinct firing glyph searched per chapter, plus one classifier pass per chapter when the pooled digit key fires. Load average 9-11 from a concurrent build | Per book, median/p90/max — **distinct needles** en 11/12/15, amh 2/5/7, fra 12/16/17, grc 12/15/17, hin 14/15/15, nya 12/19/23, spa 10/10/11, swh 12/15/18. **Hits** (occurrences of a firing glyph) en 926/4,876/7,198, amh 49/546/577, fra 1,696/6,710/10,449, grc 430/4,229/5,219, hin 996/5,341/9,360, nya 940/4,699/7,175, spa 963/4,667/7,598, swh 896/4,611/6,673. **Sites** en 2/21/69, amh 1/3/8, fra 2/10/25, grc 2/8/11, hin 2/11/32, nya 6/36/59, spa 2/7/15, swh 3/28/73. Whole-corpus locate medians: en **14.04 ms** (66 books, 4.1 MB, 265 MB/s), amh 0.38 ms, fra 20.84 ms, grc 6.12 ms, hin 24.11 ms, nya 13.34 ms, spa 10.79 ms, swh 13.69 ms — about **210 µs per book** on en against `substrate_map`'s ~500 µs per book. Corpus pattern totals 21 (amh) to 196 (nya). A variant that searched the whole book once per needle instead of once per chapter measured **slower** (en 15.33 vs 14.04 ms): the chapter lookup each hit then needs costs more than the `find_iter` call it saves | **Aho-Corasick still does not ship, and this is the row to revisit it from.** The 2026-09-03 row said to reconsider at ≥10 distinct needles per book; the tier is 10-14 at p50 and 10-19 at p90, which is exactly that line — but the needles are the *common* glyphs (comma, period, danda, Ethiopic wordspace) where the same row measured AC losing 2-6× to per-needle memmem, so the crossover it named has not actually been crossed. Revisit only if a corpus appears whose firing set is many RARE glyphs. The budget holds either way: a whole-corpus rescan is 0.4-24 ms, under the walk it may not repeat, and one book is a fifth of a millisecond |
| 2026-09-04 | D2b: what do sites cost the resident host, on a keystroke, on a warm republication, and on a judging-config flip? | M1 · `cargo bench -p usfm_galley --bench expediter`, A/B against a worktree at the pre-D2b commit 11affbb, two rounds alternating, same 66-book `testData/exampleCorpora/en_ulb` (1,189 chapters), pass `Brigade`. Load average 9-11 from a concurrent build, so the A/B ratio is the reading | Round 2 medians, **base -> D2b**: `publish_unchanged` **101.5 -> 206.1 µs** (2.0×), `edit_one_chapter_then_publish` **538.9 -> 1,028 µs** (1.9×), `publish_after_sweep_churn` **527.6 -> 1,061 µs**, `publish_cold` **33.36 -> 51.73 ms** (1.55×). Round 1 agreed: 128 -> 196 µs, 578 -> 1,013 µs, 563 -> 975 µs, 47.3 -> 51.4 ms. The new `set_config_then_publish` row — a `rarity_floor` flip and the publication after it, no map and no fold — is **3.23 ms** (1.67-3.49). The base column reproduces the 2026-09-03 D2a-2 row (91 µs / 593 µs / 526 µs / 32.5 ms) on a comparably loaded machine | Sites are the second-biggest thing a publication does, and every part of it is where it should be. `publish_cold` grows by ~18 ms, which is the 14 ms whole-corpus locate plus 66 reprojections — a cold open of a whole Bible is 52 ms. A keystroke doubles to 1.0 ms because the edited book's checksum moves, so exactly that book relocates; it is still dominated by `Pantry::update`. The +105 µs on `publish_unchanged` is NOT a rescan — no book reads text — it is recomputing 66 books' firing sets against the 71-row table and replaying 536 cached rows on every publication. That is the lever if this ever hurts: the firing set is a pure function of the aggregate and the table, so it could be cached per `RawChecksum` beside the aggregate. 206 µs is 1.3% of a 16 ms frame, so a host may still publish on a keystroke |
| 2026-09-04 | D2b gate: does the current-text site rescan agree with the pattern counts it materializes, on real scripts? | M1 · `cargo test -p sous-core --release --test sites_agree_with_counts -- --include-ignored`, every book of the committed 8-corpus tier under the default config, plus a 12-seed synthetic sweep at `support_floor` 1 | Equal everywhere, first run, no fixes: WA-en-ulb 66 books / 3,614 patterns compared / 870 occurrences, amh 27/103/35, francl 73/4,416/567, grcsr 27/680/88, hin2017 66/3,983/512, nya 62/7,796/1,126, spaRV1909 66/1,515/342, swhulb 66/5,508/933. A pattern whose glyph a book does not hold counts zero there, checked separately. The synthetic sweep reaches all four channels, both placement sides, and the pooled digit key | The roadmap's Stage 3 gate "current-text site rescan agrees with the pattern counts it materializes" is met. What it really pins is that `sites::Cursor`'s prev/next/run semantics equal the walk's on real glue and combining sequences — the two are independent implementations of charter invariants 4, 7 and 8, and they agree occurrence for occurrence over 24,600 pattern comparisons. The one place they could have diverged and did not is glue: both read a mark as `Letter` in place rather than stepping back to its base |
| 2026-09-04 | D3 item 0: what does taking digits out of runs cost and buy, on the tier and on the fleet? | M1 · `cargo run -p sous-core --release --example pattern_volume`, run against af623e2 and again after the walk change; the committed 8-corpus tier, then the 1,504-corpus vref tier | Tier, patterns total and mixed run-shape rows, before → after: WA-en-ulb 71→60 (mixed 22→15), amh 21→22 (1→2), francl 90→80 (25→19), grcsr 39→39 (7→7), hin2017 69→62 (17→12), nya 196→174 (43→39), spaRV1909 29→29 (7→7), swhulb 125→113 (35→31). Fleet totals p50/p90/p95/max 42/89/101/199 → 35/77/88/178; run-shape rows 15/29/35/52 → 9/21/25/45; exact-neighbor 8/27/34/92 → 6/24/29/85 | A digit breaks a run and joins none: `600,000` is a lone comma with `prev = Digit next = Digit`, and `130.` a lone period, instead of mixed runs of six and four. That is 15% of the tier's rows and a third of en_ulb's mixed run shapes, all of them numbers rather than conventions, and G0 and G1 now tell one story about digits. Two corpora are unchanged and amh gains one row — a split run exposes a shape that was previously buried in a longer one. Digits stay in `scalars`, in `pairs`, and in `OuterClass::Digit`; only `runs` stops seeing them |
| 2026-09-04 | D3 item 2: how many rows does G2 add once the pools come from UCD properties rather than an ASCII list? | M1 · `cargo run -p sous-core --release --example pattern_volume` with `Channels::pooled_neighbor` on, after item 0; pools generated into `core/src/unicode/pools.rs` from `PropList.txt` (`Quotation_Mark`, `Dash`, `Sentence_Terminal`, `Terminal_Punctuation`, re-pinned: xxh3-64 `86cd819867f34195`, 21,783 bytes) and GC `Ps\|Pe\|Pi\|Pf` | Tier, total (pooled): WA-en-ulb 66 (6), amh 22 (0), francl 90 (10), grcsr 41 (2), hin2017 74 (12), nya 203 (29), spaRV1909 31 (2), swhulb 130 (17). Fleet pooled-neighbor rows p50/p90/p95/max 3/12/15/32, totals 35/77/88/178 → 38/89/102/210. The table is 504 sorted `(u32, Pool)` rows, ~4 KB: `Nd` and a bare `S*` need none, and no `Nd` carries one of the four properties, so the digit lane answers before the search | G2 costs about 10% more rows and no `Class` bit — `pool_of` runs at judge time over run atoms, never in the walk. **Open, and the one thing to look at before the fleet gate:** G2 shares G3's denominator, so the two are entitled and abstain together and a pool can only fire where its members already do. Its contribution is therefore the coarser *statement* (`?` before `”`, `"`, and `’` is one Quote row rather than three exact pairs), not a fallback for an abstaining G3. If G2 is meant to speak where G3 cannot, entitlement has to become per-key, which is a change to G3's own behaviour and was not made here. **Ruled the same day (Will): option 1 — the channel ships off by default (`Channels::pooled_neighbor = false`), the pool table stays for grouping and later rules** |
| 2026-09-04 | D3 item 1: what does books-touched look like once every pattern row carries it? | M1 · `cargo run -p sous-cli --release -- --report debug/d3-en_ulb.html testData/exampleCorpora/en_ulb`, 66 books, 67 patterns, 404 sites | Books-touched per row: min 1, p50 **2**, p90 9, max 30 of 66. **32 of 67 rows (48%) name a single book.** Channel split on that page: 19 exact-neighbor, 6 pooled, 17 run-shape, 19 placement, 6 rarity | Dispersion is worth publishing and not worth gating on: half the corpus's rows are one-book facts, which is exactly the shape a reviewer wants ranked and exactly the shape a threshold would silence — a one-book row can be a typo or a genre. It rides the row as a `u8` at byte 22, books-possible is the header's `book_count`, and nothing in the engine reads it |
| 2026-09-04 | W1 (a): what does the real word row cost per chapter and per Bible, against the 2026-09-02 lane-B estimate of 3.2-4.1 MB? | M1 · `cargo run -p sous-core --release --example observation_size`, `WordRow::resident_bytes()` per chapter over the committed tier | `size_of::<WordCount>()` is **24 B**, not the 12 B lane B assumed, so a cased Bible's rows are **6.4-8.2 MB**, about 2x the estimate: en_ulb 6.41 MB (median 5,304 B/chapter, p90 7,752), francl 8.23 MB (5,976 / 9,072), swhulb 7.07 MB (5,784 / 8,856), spaRV1909 6.53 MB (5,328 / 8,160), nya 6.43 MB (6,240 / 9,672), grcsr 1.80 MB over 260 chapters (6,360 / 10,416). Uncased scripts pay nothing at all: hin2017 0.029 MB (median 24 B, the empty row's inline size), amh 0.010 MB (median 48 B) | the estimate under-counted by exactly the two lanes it left out — the four `u16` free counters and the forced counter. 24 B is the honest number, and chapter grain at 6-8 MB is above the phone budget the decision assumed; book grain (the recorded fallback) is the lever if that budget bites. Nothing here changes the rule |
| 2026-09-04 | W1 (b): what does the word walk cost per book, against `substrate_map`? | M1 · `cargo bench -p sous-core -- map`, median, over the committed tier | µs/book, word walk vs substrate walk: en_ulb **618** vs 423, francl **699** vs 590, grcsr **636** vs 413, spaRV1909 **608** vs 478, swhulb **595** vs 377, nya **548** vs 344 — and, uncased, hin2017 **303** vs 733, amh **95** vs 226. Throughput 99-104 MB/s on cased Latin/Greek, 490 MB/s on Ethiopic and Devanagari | the word walk is 1.2-1.6x the substrate walk on cased scripts, which is the cost of a second scalar pass plus one xxh3 per word; on uncased scripts it is 2.4x *cheaper* than the substrate walk, because it hashes nothing and stores nothing. "Uncased scripts pay nothing" is measured, not assumed |
| 2026-09-04 | W1 (c): does the word rule agree with UAX #29, and where does it not? | M1 · `cargo run -p sous-core --release --example word_breaks`, our word spans vs `unicode_segmentation::split_word_bound_indices`, the 8-corpus tier then all 1,504 fleet corpora | Tier: amh 0.003%, grcsr 0.056%, spaRV1909 0.094%, swhulb 0.106%, WA-en-ulb 0.250% (`twenty-five`, `Ben-Hadad`, `father-in-law`), nya 0.353% (`zo-ona`, reduplication), hin2017 0.658% (`अपने-अपने`), francl 1.882% (`lui-même`, `Jésus-Christ`, and elisions). Fleet, overall share by dominant script: Greek 0.033%, Hangul 0.061%, Gujarati 0.095%, Kannada 0.099%, Malayalam 0.191%, Oriya 0.208%, Gurmukhi 0.448%, Devanagari 0.467%, Arabic 0.469%, Telugu 0.516%, Bengali 0.566%, Cyrillic 0.712%, Sinhala 1.44%, **Latin 1.63%** (1,288 corpora, p50 0.084%, p90 3.99%), Ethiopic 1.94%, Tamil 2.95% (p90 40.9%), Syriac 0.000%, **Hebrew 8.65%** (p50 13.5%) — then the spaceless scripts: Tibetan 47.2%, Myanmar 89.5%, Khmer 96.2%, Thai 98.0%, Han 99.4%, Kana 99.8%, Lao 99.8% | every spaced script agrees on 98%+ of words, and the disagreements are the hyphen and apostrophe joins the rule was written for — UAX 29 splits them, we join them, and joining is what a casing convention wants. Hebrew's 8.65% is the maqaf `־`, which UAX 29 splits: worth revisiting if a Hebrew casing lane ever existed, and it cannot, since Hebrew is uncased. The spaceless scripts confirm the abstention: neither rule works there without a dictionary, and no word lane may claim anything in them. No runtime fallback is warranted for any script we judge |
| 2026-09-04 | W1: how many rows does the casing channel add beside the glyph channels, at the placeholder defaults? | M1 · `cargo run -p sous-cli --release -- --findings testData/exampleCorpora/en_ulb` | 430 casing rows and 1,461 casing sites against **61** glyph rows over the same 66-book corpus. The widest: `will` 26 of 12,017 free uses written `Will`, in 11 of 66 books; the narrowest are one-off `Title` rows on words seen 13-30 times. `Stop` fires 4/76 because an opening quote after a COMMA is a free position under the current rule | 7x glyph volume, which reproduces v1's "roughly eight times" measurement on this corpus and confirms the block in `rules/word-conventions.md`: the word floor and band column must be calibrated on the fleet before any of this ships as a default. The `Stop` case is the shape of that calibration — v1 had a dial that let comma-following count as forced, and the fleet run has to decide whether this rule needs one |
| 2026-09-04 | W1: what do words cost the resident host — keystroke, unchanged republish, cold, config flip, and resident bytes? | M1 · `cargo bench -p usfm_galley --bench expediter`, same 66-book `testData/exampleCorpora/en_ulb` (1,189 chapters), pass now `Brigade` = `(HygieneBytes, Substrate, Words)`, uncommitted on 730406e; `cargo run -p usfm_galley --release --example working_set 2>&1 \| tail -40`. Load average 13-20 from heavy concurrent load (25 users on the box), well above the D2b comparator's 9-11 | Medians (D2b in parentheses): `publish_unchanged` **5.03 ms** (206.1 µs), `edit_one_chapter_then_publish` **6.76 ms** (1,028 µs), `publish_after_sweep_churn` **6.69 ms** (1,061 µs), `publish_cold` **142.8 ms** (51.73 ms), `set_config_then_publish` **13.44 ms** (3.23 ms). Sous resident bytes **0.44 MiB** (0.28 MiB pre-sites), `Expediter::resident_bytes()` total 20.35 MiB, allocator peak **57.34 MiB** at 200 keystrokes | **Real, not load.** A second run on the same box with no competing build (load 17-19, all of it background system processes) gave the same medians: `publish_unchanged` 4.92 ms, `edit_one_chapter_then_publish` 6.45 ms, churn 6.46 ms, `publish_cold` 137.6 ms, `set_config_then_publish` 13.7 ms. Words cost lands in the corpus-level judge, not in storage or replay: `publish_unchanged` folds nothing and locates nothing, yet grew 0.2 -> 4.9 ms, because every publication re-merges every book's word rows (tens of thousands of distinct hashes across 66 books) into corpus totals and rejudges them; `publish_cold` carries the second per-chapter walk on top. 4.9 ms is 30% of a 16 ms frame on an M1 in native code, so this does NOT stay under the phone-in-wasm budget as is. The lever is incremental word totals: keep the corpus tally keyed by hash and, when one book's aggregate changes, subtract its old row and add its new one instead of re-merging 66 books — the same lever D2a-2 named for glyphs, now load-bearing. The Sous cache itself grew only 0.28 -> 0.44 MiB |
| 2026-09-04 | W1 grain: what does chapter grain for word rows cost in memory and buy per keystroke, against book grain? | M1 · `cargo run -p sous-core --release --example word_grain`, the committed 8-corpus tier; per book, chapter grain sums `WordRow::resident_bytes()` over every chapter mapped separately and folds them, book grain joins the book's chapters with `"\n"` and maps/folds that once; keystroke cost is median-of-5 (chapter grain: re-map the book's median-sized chapter + re-fold the book from cached rows; book grain: re-walk the whole book + fold) | MB/Bible, chapter vs book grain (book grain's cost first, saved µs/keystroke second — median over books): WA-en-ulb 6.409 vs 2.027 MB, saves 203.3 µs; francl 8.234 vs 3.232 MB, saves 249.6 µs; swhulb 7.073 vs 3.218 MB, saves 190.7 µs; spaRV1909 6.532 vs 2.604 MB, saves 208.7 µs; nya 6.427 vs 3.355 MB, saves 208.4 µs; grcsr 1.796 vs 0.966 MB, saves 147.0 µs; hin2017 0.029 vs 0.002 MB, saves 134.3 µs; amh 0.010 vs 0.001 MB, saves 26.0 µs. Every corpus: chapter grain costs more MB and buys back more µs per keystroke than book grain. `size_of::<WordTotal>() = 32 B`; `WordAggregate` has no `resident_bytes()`, estimated as `size_of_val(agg.words())` — 488-617 entries / 13.3-19.7 KB on cased books, 0-32 B on uncased ones, the same on either grain since the two rescans agree | The trade is real and the same shape on every corpus: chapter grain pays 0.8-5.0 MB more per Bible (2-3x book grain) to buy back 130-250 µs on every keystroke, because book grain's keystroke cost is dominated by re-walking the whole book's text instead of one chapter. Chapter-grain memory here (6.4-8.2 MB on cased Latin/Bantu/Romance) reproduces the 2026-09-04 W1 (a) row exactly, since that row's `real_word_row_bytes` sum is the same computation. Whether the trade is worth it is a host question — a resident desktop host with a 16 ms frame budget can absorb either grain's keystroke cost, but a phone-in-wasm budget that already flagged chapter grain's memory (W1 (a)) should take book grain's ~3x smaller footprint and eat the slower re-walk, since 130-250 µs is still two orders of magnitude under a frame |

## Probe evidence behind the starting architecture

These are v2 exploratory probes, run before the production crates existed.
Probe entry points live under `spikes/probes`; `cargo test -p probes` and the
Criterion benches are the local starting points.

| date | question | observed result | consequence |
| --- | --- | --- | --- |
| 2026-09-03 | D2b scoping: what does reprojecting a book from cached products cost, against retaining projected text? | M1 · `cargo run -p usfm_galley --release --example projection_cost`, 66-book `en_ulb` (4,506,101 raw bytes), best of 20 per book, `OnionBook::from_parts(text, mask, toc)` vs `OnionBook::parse(text)` | Reprojection from cached mask + TOC: **38 µs/book** average, 2.5 ms for all 66, 1.7 GB/s of raw text. Full re-derivation (lex, CST, mask, TOC, projection): 132 µs/book, 8.7 ms total. Projected text is 4,116,999 bytes, 91% of raw | A site rescan reprojects the books it needs per call; retaining projected text would double resident text for a saving of ~38 µs on a path already costing ~435 µs. Rule: no second copy |
| 2026-09-03 | D2a-2 scoping: how big is a rarity roster with letters in it, and where does the logographic abstention bound sit? | M1/M2 · `cargo run -p sous-core --release --example inventory_census`, corpus-wide sums of the substrate `scalars` lane; the 8-corpus tier, then the 1,504-corpus vref tier (found at the sibling `scripture-sous-chef/corpora/vref`; `corpora/calibration-corpora/` is not on this machine) | Tier: distinct letters 44 (hin) to 238 (amh); letters below count 5: 0-5 for Latin/Devanagari, 12 Greek, 17 Amharic; nonletters below 5: 0-8. Fleet, roster at floor 5 (median / p90 / max): Latin 2/5/45 letters and 1/5/18 nonletters over 1,288 corpora; Cyrillic 3/7/13; Devanagari 1/7/15; Arabic 2/5/7; Hebrew 0/4/14; Greek 12/17/17; Ethiopic 12/24/29; **CJK/Han 728/814/814** over 6 corpora with 3,096-3,463 distinct letters. Distinct letters: every alphabet's max is under 300 (Ethiopic 281); every logographic corpus is above 3,000; an "Other" bucket of 91 corpora has a 1,358 outlier | A letter roster is real for alphabets and pure noise for logographic scripts, with an empty band between ~300 and ~3,000 distinct letters. Default `letter_roster_bound` = 500 distinct letters: above it the roster abstains from letters and says so. Nonletter rosters are tiny everywhere (max 18), so the roster is not a volume risk. Open for the fleet gate: Ethiopic and Greek roster 12-29 letters at floor 5, and some of those are legitimate rare syllables, so letters may want a lower floor than nonletters |
| 2026-09-03 | D2b scoping: for a site rescan over N needle patterns, when does memchr-per-needle lose to memmem-per-needle, and when does one Aho-Corasick pass beat both? | M1 · throwaway divan crate (source kept as `experiments/needle-search.rs`), whole-corpus text of en_ulb (4.1 MB), hin2017 (9.5 MB), amh (1.25 MB); N ∈ {1,4,10,40}; needle sets built from real counts: rare (<50 hits), common (1-3% density: comma, danda, Ethiopic wordspace), two-scalar pairs; `Substrate.map` over the same text as the ceiling | Rare needles: every engine sits near the byte-scan ceiling (80 GB/s at N=1) and per-needle engines fall linearly with N (memmem N=40: 0.7-0.8 GB/s) while AC stays flat (17-25 GB/s at N=40). Common needles are hit-bound, not scan-bound: English comma 55k hits costs memchr 1.08 ms / memmem 1.20 ms / AC 2.2 ms per corpus; Hindi danda 245k hits costs memmem **4.4 ms** but memchr **25 ms** and AC 7.2 ms. Pairs: memmem wins at every N (English `,"` 0.68 ms vs memchr 0.82 vs AC 1.08). AC beats memmem only at N≥10 on rare sets, and at N=40 on common English it collapses to 27 ms. Walk ceiling: 32 ms en, 38 ms hin, 5 ms amh per corpus (0.5 ms per book). AC-DFA within 5% of default everywhere | **Two verdicts.** (1) memchr on a UTF-8 lead byte is a trap outside ASCII: every Devanagari scalar shares lead byte 0xE0, so "find the danda" verifies every character and runs 6× slower than memmem. The rescan uses `memmem::Finder` per needle, which is memchr internally for one-byte needles, so nothing is lost on Latin. (2) Aho-Corasick earns its dependency only with ≥10 distinct needles per book on rare glyphs, and the firing set is p50 10 / p90 36 patterns per *corpus* sharing glyphs, so distinct needles per book will be under that; ship memmem per needle, measure needles-per-book in D2b, and revisit AC from this row if it is often ≥10. Budget check: a full-corpus rescan on common needles is 1-5 ms plus the per-hit cursor, under the 33 ms walk and well under a frame; per book it is tens of µs |
| 2026-09-03 | what is the host's real working memory over a 66-book corpus, at rest and under keystroke churn, by owner? | M1 · `cargo run -p usfm_galley --release --example working_set`: a counting `#[global_allocator]` (live, peak, calls) around `Expediter<Brigade>` over `en_ulb` (4.30 MiB raw), 64 MiB Warmer budget, 200 single-character keystrokes cycling ten books with a publish after each | Live heap, host only: 23.7 MiB after registering 66 books (no chapter mapped), 25.2 MiB after the cold publish, unchanged after an idle publish, 26.7 / 27.2 / 28.2 MiB after 50 / 100 / 200 keystrokes; peak 28.5 MiB, never more than 0.35 MiB above live. 320 allocation calls per keystroke+publish, flat. `Expediter::resident_bytes()` reports 12.8 MiB of that: retained text 4.30, Pantry products 2.04 (residual), Warmer 6.21, Sous caches 0.28 (1,229 observations, 106 chapter tables, aggregates). **10.3 MiB (44%) is unattributed**: `Warmer::resident_bytes` is a documented estimate that omits the CST/token products it holds. Gaps named: no `Expediter::pantry_mut`, no `Pantry::text_bytes`, `Fingerprint::resident_bytes` private | The host runs at ~5.5× raw text, of which Sous itself is 0.3 MiB: the analysis caches are not the memory story, Onion's retained products in the Warmer are. Growth under churn is ~10 KB per keystroke and did not plateau in 200 edits because the Warmer's 64 MiB budget was never reached — content-addressed chunks of every edited state accumulate until it is. Two follow-ups before wasm: make `Warmer::resident_bytes` exact so the budget means what it says, and pick a wasm default budget from this row (linear memory never shrinks, so peak is the number). Unattributed bytes must reach zero before any memory claim is made |
| 2026-09-03 | after making `Warmer::resident_bytes` exact, how much of the host's heap is accounted for, and does the Warmer budget bite at 16 / 32 / 64 MiB? | M1 · `SOUS_WARMER_MIB={16,32,64} cargo run -p usfm_galley --release --example working_set`, same en_ulb run as the row above; `product_bytes` now sums `capacity() * size_of::<T>()` over every owned Vec in a chunk's CST, tokens, and lint report, plus the map's own rows | Warmer real heap **12.97 MiB** (was declared 6.21); Pantry text 4.30, Pantry products 2.65, Sous caches 0.28; declared 20.19 of 23.10 MiB live. **Unattributed 10.3 → 2.9 MiB (12.6%)**; the rest is the Pantry's own map rows and cloned ids, a `pub(crate)` Vec set inside onion's lint `Carried` (small), and allocator size-class rounding over ~140k allocations. All three budgets gave identical numbers: 4,040 hits / 1,455 misses, peak 28.55 MiB, keystroke+publish median ~610 µs in the example harness — the Warmer never reached 16 MiB in 200 keystrokes, so no eviction happened | The Warmer's weight was `len()` × magic constants where it should have been `capacity()` × `size_of`; eviction was bounding an undercount by half. Now the budget means real bytes. A whole Bible's current-state chunk products are ~13 MiB, so any budget at or above 16 MiB is history only, and 200 keystrokes did not exercise eviction at all: choosing wasm defaults needs a longer churn (thousands of keystrokes across books) to see hit rate and re-derivation cost under pressure. That run, not opinion, sets the number |
| pre-v2 | byte hygiene cost | SWAR range scans about 5.3 GiB/s; fixed needles 2.3-41 GiB/s across stress corpora | rescan; do not retain hygiene state initially |
| pre-v2 | stream versus scalar tape | streaming was about 1.3-2.3× faster; tape cost worsened with corpus size | no ambient materialized tape |
| pre-v2 | full shared substrate | about 28 ms on the English probe corpus versus roughly 257 ms v1 cold analysis | simple whole-corpus/whole-book passes are viable |
| pre-v2 | expanded feature substrate | roughly 12-31% above the first shared-counter cut | counter-shaped additions can share the walk cheaply |
| pre-v2 | grapheme atom differential | after fixing extender classification, one nonletter-involving mismatch over 1,504 corpora | fast walk plus grapheme-safe emission is viable |
| pre-v2 | per-chapter row reduce | about 17-25 µs for 8k-12k glyph rows; edit plus reduce under 50 µs | derive aggregates on read; no rollup cache |
| pre-v2 | band sweep | default staircase gave about p50 10, p90 36, p95 44 rows/corpus; low cloud tracked `0.8/sqrt(n)` | readable fraction bands are credible shipping candidates |
| pre-v2 | dispersion | clustering changed gradually with share; genre remained a confound; above-band clustered rows about 0.3/corpus | annotation/ranking only, never a hard gate |
| pre-v2 | packed v1 wire donor | fixed 16-byte buffers made wasm/transfer/decode nearly flat and far cheaper than object arrays in the old measurements | preserve fixed binary snapshots, but redesign addressing |
| pre-v2 | proportionality paired survey | project scope covered small books; 10-20% chops were nearly invisible; source choice materially changed results | keep dual scopes and narrow claims; reproduce before defaults |

The old repository's calibration notes remain historical evidence in git and in
the donor checkout; they are intentionally not duplicated here.
| 2026-09-04 | W1b: what do book-grain word rows and a resident corpus tally cost the host, against the W1 row measured the same day? | M1 · `cargo bench -p usfm_galley --bench expediter` once, then `cargo run -p usfm_galley --release --example working_set` once; same 66-book `testData/exampleCorpora/en_ulb` (1,189 chapters), pass `Brigade`, uncommitted on 730406e. Load averages 9.3 falling to 8.2 across the bench and 6.8-7.2 across `working_set`, against the W1 row's 13-20 | Medians (W1 in parentheses): `publish_unchanged` **1.153 ms** (5.03), `edit_one_chapter_then_publish` **4.154 ms** (6.76), `publish_after_sweep_churn` **4.084 ms** (6.69), `publish_cold` **134.6 ms** (142.8), `set_config_then_publish` **9.66 ms** (13.44). Memory, ground truth from the counting allocator: peak live heap **44.54 MiB** (57.34), live 43.67 MiB at 200 keystrokes; Sous cache **1.08 MiB** (0.44) and `Expediter::resident_bytes()` **21.00 MiB** (20.35), the growth being the tally's own rows now counted (~0.6 MiB, ~14k distinct case-folded words at 48 B). `working_set`'s keystroke median 4,407 µs over ten cycled books. A keystroke now re-maps the edited book whole — 16 chapters for MRK, not 1 | The merge is gone and the memory came back: **-22% peak heap (-12.8 MiB)** for +0.6 MiB of tally, and a warm republication is **4.4x faster**. `publish_unchanged` did NOT reach the 0.2 ms target, and the 1.15 ms left is not the merge — an unchanged publication does not touch the tally at all. What remains on that path is the site cache's own work, unmeasured apart here: `firing` over a ~490-row table for all 66 books (430 of those rows are casing, one binary search into the book's ~5k word rows each) and the replay of ~1,900 cached site rows. That is exactly the lever the 2026-09-04 D2b row already named — cache the firing set per `RawChecksum` beside the aggregate — and `set_config_then_publish` is the same work plus real relocation. Neither is in this slice. Published bytes are unchanged by construction and by test: `galley/tests/equivalence.rs` passes whole — both synthetic seeds, the `Brigade` churn, and the ignored whole-Bible oracles — and gains a recased chapter and a `channels.casing` flip, each compared to a cold `analyze_with` |
| 2026-09-04 | Warmer budget sweep under real eviction: what does 8/16/32/64 MiB cost once the churn is long enough to actually evict, against the 2026-09-03 row where no budget ever bit? | M1 · `SOUS_WARMER_MIB={8,16,32,64} cargo run -p usfm_galley --release --example working_set`, `working_set.rs` extended to 10,000 keystrokes round-robin across all 66 books of `en_ulb` (period 66 — every book revisited ~151 times, edit A, edit B, ... back to A), one more character inserted at the same middle-chapter text-token offset each visit; new `Warmer::evictions()` accessor added, no policy change. `uptime` 1-min load before→after each run: 8 MiB 8.6→7.8, 16 MiB 7.8→6.8, 32 MiB 6.8→5.1, 64 MiB 4.8→13.3 (a concurrent process started mid-run, so that row's absolute numbers are the least clean of the four) | Live/peak heap MiB at 1k/5k/10k keystrokes — 8 MiB: 73.6/75.4, 74.1/75.8, 74.1/75.8; 16 MiB: 82.2/83.9, 82.6/84.3, 82.6/84.3; 32 MiB: 85.9/87.1, 99.6/101.3, 99.6/101.3; 64 MiB: 85.9/87.1, 122.5/123.9, 133.5/135.3 — 8 and 16 MiB plateau by 1k keystrokes, 32 and 64 keep climbing because there is more history for them to hold. Warmer's own `resident_bytes()` at 10k: 8.06, 16.13, 32.31, 64.74 MiB, each sitting right at its declared ceiling (the excess over the budget is `map_bytes`, the map's own row overhead, which the budget does not bound). Hits/misses/evictions of 191,697 lookups: **8 MiB 0 / 191,697 / 189,281 — zero hits** across the whole run; 16 MiB 179,686 / 12,011 / 8,720; 32 MiB 179,686 / 12,011 / 7,024; 64 MiB 179,686 / 12,011 / 3,605. 16/32/64 give IDENTICAL hit/miss counts despite evictions falling from 8,720 to 3,605 as budget grows: above 16 MiB, eviction only ever removes stale edit-history versions the round-robin never asks for again, never anything the corpus's current-state working set (~13 MiB, 2026-09-03 row) still needs. Splitting the misses by whether every chunk of the touched book missed at once (a whole-book re-derivation) or only the edited one (a chunk re-lex): 8 MiB is **190,442 misses / 10,000 events, all whole-book — every single keystroke, no exceptions**; 16/32/64 MiB all show 9,244 chunk-relex misses (9,244 events) against 1,512 whole-book misses (756 events), and 756 is exactly `5 uncached books (≤2 chunks each, permanently uncached by design) × ~151 visits` — i.e. **zero LRU-driven whole-book re-derivations at any budget ≥16 MiB**, only the structurally-uncached small books. First eviction at keystroke 1 (8 MiB), 586 (16), 2,458 (32), 6,173 (64). keystroke+publish median/p99: 8 MiB 2,681/12,450 µs; 16 MiB 2,558/11,180; 32 MiB 2,572/11,194; 64 MiB 2,545/11,088 µs — only 8 MiB pays a visible time cost, +5% median / +11% p99 over the rest, consistent with a whole-book re-derivation costing ~132 µs/book (2026-09-03 D2b scoping row) against a ~2.5 ms keystroke that `Pantry::update`'s own per-book remap already dominates | 16 MiB is confirmed as the real floor, not a guess: it already holds `en_ulb`'s whole current-state working set with slack, so 16/32/64 behave identically for this corpus and this churn — the extra budget only buys more retained history nobody revisits, and the plan's original guesses (phone 16, laptop wasm 32, desktop 64) survive with evidence behind them, chosen as floor / one Bible's history / several. **8 MiB answers the pin-current-state question directly: yes, the current state itself is evicted.** Zero hits of 191,697 lookups means every visit to every book, not just the one just edited, pays a full re-derivation — the round-robin comes back around before that book's own last-known-good chunks survive. A budget below the ~13-16 MiB floor needs a pin-current-state policy (evict history only, never the resident book's own latest products); above the floor, plain LRU-by-bytes already behaves as if history alone were being deprioritized, so no policy change is owed there |
| 2026-09-04 | what is the ~58 MiB of live heap the Warmer/Pantry/Sous accessors do not account for? | M1 · `cargo run -p usfm_galley --release --example heap_profile` (new, dev-dep `dhat = "0.3.3"`; reuses `working_set`'s corpus load and edit-point recipe verbatim, `SOUS_WARMER_MIB=8`, 1,000 round-robin keystrokes — the gap plateaus by then per the row above), `dhat::Profiler::new_heap()` behind `#[global_allocator] dhat::Alloc`, JSON parsed directly for `gb` (bytes live at global max) per allocation site, no viewer. Cross-checked against unmodified `SOUS_WARMER_MIB={8,64} cargo run -p usfm_galley --release --example working_set` (10,000 keystrokes) run the same day for the authoritative accessor totals | dhat t-gmax **75.24 MiB** (matches `working_set`'s reported peak of 75.4 MiB at 8 MiB budget almost exactly). Top 15 sites by bytes-live-at-gmax cover 71.33 MiB (94.8% of gmax), by owner — **sous 44.53 MiB**, **onion 13.90 MiB**, **harness 12.89 MiB**: (1) `sous_core::words::fold::fold_book`, via `Expediter::publish` — **43.16 MiB**, 331 blocks — sous; (2) `heap_profile::main` direct (harness `live_texts` growth) — 9.01 MiB, 66 blocks — harness; (3) `usfm_onion::scanner::Scanner::new` via `Warmer::parsed` — 6.69 MiB, 915 blocks — onion; (4) `Expediter::update_with` (Pantry's retained text copy) — 4.51 MiB, 66 blocks — onion; (5) `std::fs::read_to_string` (harness corpus load) — 4.51 MiB, 66 blocks — harness; (6) `sous_core::words::totals::WordTotals::apply` — 1.35 MiB, 1 block — sous; (7) `usfm_onion::mask::mask` `Vec<Range<u32>>` — 0.86 MiB, 66 blocks — onion; (8) `usfm_onion::cst::build_chunk` via `Warmer::parsed` — 0.72 MiB, 915 blocks — onion; (9) `fold_book`'s scratch `rows` vec — 0.70 MiB, 1 block — sous; (10) `usfm_onion::toc::toc` `Vec<Cursor>` — 0.69 MiB, 66 blocks — onion; (11) `mise::utf16::utf16_table` — 0.54 MiB, 66 blocks — onion; (12) `sous_core::substrate::Substrate::map` (chapter rows) — 0.54 MiB, 1,449 blocks — sous, already counted (matches `resident_observations`); (13) `sous_core::words::walk::Scan::close` scratch — 0.44 MiB, 150 blocks — sous; (14) `usfm_onion::mask::mask` `Vec<u32>` — 0.43 MiB, 66 blocks — onion; (15) `hashbrown::RawTable::reserve_rehash` on the observations map — 0.41 MiB, 1 block — sous. Site (1)'s 331 blocks track `resident_tables=330`: one `WordAggregate` per retained book generation. `Expediter::resident_bytes()` (16.51 MiB @ 8 MiB, matching `working_set`) leaves 48.95 MiB unattributed at 8 MiB budget and 51.75 MiB at 64 MiB — both close to onion+sous's dhat total here, confirming "constant across budgets" (the gap isn't in the Warmer's budget-bound chunk cache at all) | **Found: `Expediter::resident_bytes()`'s own doc comment names the hole — "an aggregate...count[s] its own inline size and not the heap a pass hangs off them" — and `words::mod.rs`'s `cached` line literally does `self.aggregates.len() * size_of::<P::Aggregate>()`, i.e. `size_of::<WordAggregate>()` (~32 B, the `Vec` header), never `.words.len() * size_of::<WordTotal>()`. Site (1) alone (43.16 MiB, one `WordAggregate` per retained-generation `fold_book` call) is 74-88% of the ~49-52 MiB gap, dwarfing the ~1.3-1.6 MiB `sous_cache` figure `working_set` already reports. **Reading:** of the "unattributed" total, harness overhead (corpus + `live_texts`, ~12.9 MiB here) is not shipped and already excluded from `working_set`'s own `host_live`; onion's share (~13.9 MiB: Warmer scanner/CST buffers, Pantry text/mask/toc/utf16) is real engine state a wasm host carries, but it is already visible through `Warmer::resident_bytes()` and `Pantry::text_bytes()`/`resident_bytes()` — the top-15 numbers reconcile with those accessors' own figures. The genuinely *invisible* engine state — and the largest single item in this whole table — is the per-book `WordAggregate` cache inside `self.aggregates`: real, resident, budget-independent heap that `resident_bytes()` undercounts by roughly 43 MiB on this corpus. Fix is mechanical: add `resident_bytes()` (or an equivalent size hook) to `ChapterPass::Aggregate` and sum real heap instead of `size_of::<P::Aggregate>()` at that one line |
| 2026-09-06 | W1d: does `ChapterPass::aggregate_bytes` plus pruning a book-grain pass's aggregate to its book's current checksum close the gap the 2026-09-04 dhat row found? | M1 · `SOUS_WARMER_MIB=16 cargo run -p usfm_galley --release --example working_set` once (`uptime` before: load averages 13.18/9.40/6.59, 24 users), same `en_ulb` 66-book corpus, pass `Brigade`, on top of 7f7cacd; plus a new counting-allocator integration test, `galley/tests/aggregate_accounting.rs`, one cold publish of the same corpus | 16 MiB budget, 10k keystrokes (2026-09-04 sweep row in parentheses): live **49.22 MiB** (82.6), peak **50.97 MiB** (84.3) — **-39.5% peak**. `Expediter::resident_bytes()` **32.74 MiB** against the allocator's host-only live **40.62 MiB**, 19.4% unattributed (Pantry text 4.31 MiB, Pantry products 2.65 MiB, Warmer 16.13 MiB, Sous cache 9.65 MiB); `resident_observations=1449 resident_tables=330 last_folded=1`. The allocator-checked test, a cold publish from empty: claimed **28.56 MB** against the allocator's delta of **30.43 MB**, off by **6%** — inside the 10% target. The undo test (`a_book_grain_pass_keeps_one_aggregate_per_book_through_edits_and_an_undo`): `resident_aggregates()` is **2** (GEN plus MRK) through four edits and after an undo to the first version — against 2×5=10 before this pass, since `Brigade` retains five generations (`DEFAULT_GENERATIONS + 1`) per book; the undone publication is byte-identical to the first, and `last_mapped() > 0` on the undo, because the pruned aggregate forces a re-map and a re-fold | The 43.16 MiB `WordAggregate` site the dhat row found is gone from what `resident_bytes()` misses, and the 16 MiB sweep row's peak fell from 84.3 to 50.97 MiB with nothing else in the harness changed — close to the plan's "peak 75 → ~40 MiB" guess (a different budget/keystroke count, so not the same run, but the same shape). The remaining 19.4% (7.88 of 40.62 MiB) is NOT this slice's gap: it is Pantry's own map rows, cloned ids, and allocator size-class rounding, the same category the 2026-09-03 row already named at 12.6% on a smaller corpus — closing it is a separate pass. An undo beyond a book-grain pass's current checksum now costs a re-map and a re-fold (~600 µs on `en_ulb`, `expediter.md`) instead of a free replay, which is the trade the spec asked for: the aggregate no longer rides five generations deep for every book edited once and never touched again |
